from World import *
from PathFinder import *
   
def PathWorld(width,height,depth):
    return World(width,height,depth)
def PathFinder(world, start, end, preCrumbsList, xInterval, yInterval, zInterval):
    return Path_Finder(world, start, end, preCrumbsList, xInterval, yInterval, zInterval)